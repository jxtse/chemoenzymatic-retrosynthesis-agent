

- Use `uv` as python package management tools. 
    - `uv add` for adding new packages
    - `uv sync` for installing added new packages
    - `uv run` for running python scripts
    - Updating the `pyproject.toml` file as the project progresses